<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_hr_anonymous_message_search_employee" model="ir.ui.view">
            <field name="name">hr.anonymous.message.search.employee</field>
            <field name="model">hr.anonymous.message</field>
            <field name="priority">20</field>
            <field name="arch" type="xml">
                <search string="Search My Messages">

                    <field name="name" string="Subject"/>
                    <field name="category_id" string="Category"/>

                    <filter string="Draft" name="filter_draft"
                            domain="[('state', '=', 'draft')]"/>
                    <filter string="Sent" name="filter_sent"
                            domain="[('state', '=', 'sent')]"/>
                    <filter string="In Progress" name="filter_in_progress"
                            domain="[('state', 'in', ['acknowledged', 'in_progress'])]"/>
                    <filter string="Resolved" name="filter_resolved"
                            domain="[('state', '=', 'resolved')]"/>
                    <filter string="Closed" name="filter_closed"
                            domain="[('state', 'in', ['declined', 'closed'])]"/>
                    <separator/>
                    <filter string="Still Open" name="filter_open"
                            domain="[('state', 'in', ['sent', 'acknowledged', 'in_progress'])]"/>
                    <filter string="Completed" name="filter_completed"
                            domain="[('state', 'in', ['resolved', 'declined', 'closed'])]"/>
                    <separator/>
                    <filter string="This Month" name="filter_this_month"
                            domain="[('create_date', '&gt;=', context_today().strftime('%Y-%m-01'))]"/>
                    <filter string="Date Sent" name="filter_create_date"
                            date="create_date"/>
                    <separator/>
                    <filter string="Group by Category" name="group_category"
                            context="{'group_by': 'category_id'}"/>
                    <filter string="Group by Status" name="group_state"
                            context="{'group_by': 'state'}"/>
                    <filter string="Group by Priority" name="group_priority"
                            context="{'group_by': 'priority'}"/>
                    <filter string="Group by Month" name="group_month"
                            context="{'group_by': 'create_date:month'}"/>

                    <searchpanel>
                        <field name="state"       string="Status"   icon="fa-circle" select="one" enable_counters="1"/>
                        <field name="category_id" string="Category" icon="fa-tag"    select="one" enable_counters="1"/>
                    </searchpanel>

                </search>
            </field>
        </record>

        <record id="view_hr_anonymous_message_list_employee" model="ir.ui.view">
            <field name="name">hr.anonymous.message.list.employee</field>
            <field name="model">hr.anonymous.message</field>
            <field name="priority">20</field>
            <field name="arch" type="xml">
                <list string="My Anonymous Messages" create="1" edit="0" delete="0"
                      default_order="create_date desc">
                    <field name="create_date" string="Sent Date"  optional="show"/>
                    <field name="category_id" string="Category"   optional="show"/>
                    <field name="name"        string="Subject"/>
                    <field name="priority"    widget="priority"   optional="show"/>
                    <field name="state"       widget="badge"
                           decoration-info="state in ['acknowledged', 'in_progress']"
                           decoration-warning="state == 'sent'"
                           decoration-success="state == 'resolved'"
                           decoration-danger="state == 'declined'"
                           decoration-muted="state in ['closed', 'draft']"/>
                </list>
            </field>
        </record>

        <record id="view_hr_anonymous_message_form_employee" model="ir.ui.view">
            <field name="name">hr.anonymous.message.form.employee</field>
            <field name="model">hr.anonymous.message</field>
            <field name="priority">20</field>
            <field name="arch" type="xml">
                <form string="My Anonymous Message" create="1" edit="1" delete="0">
                    <header>
                        <button name="send_to_hr" string="Send to HR" type="object"
                                class="oe_highlight"
                                invisible="state != 'draft'"/>
                        <button name="action_close_ticket" string="Close This Message"
                                type="object"
                                confirm="Are you sure you want to close this message?"
                                invisible="state in ['draft', 'closed']"/>
                        <field name="state" widget="statusbar" readonly="1"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="category_id" required="1"
                                       readonly="state != 'draft'"/>
                                <field name="priority" widget="priority"
                                       readonly="state != 'draft'"/>
                            </group>
                            <group invisible="state == 'draft'">
                                <field name="create_date" string="Sent On" readonly="1"/>
                            </group>
                        </group>
                        <group>
                            <field name="name"
                                   placeholder="Brief subject (e.g., 'Workplace concern')"
                                   required="1" readonly="state != 'draft'"/>
                        </group>
                        <notebook>
                            <page string="Your Message">
                                <field name="description" widget="html"
                                       placeholder="Write your detailed anonymous message here."
                                       required="1" readonly="state != 'draft'"/>
                            </page>
                            <page string="Resolution"
                                  invisible="state not in ['resolved', 'declined']">
                                <group>
                                    <field name="resolution_notes" readonly="1" nolabel="1"/>
                                </group>
                            </page>
                        </notebook>
                        <field name="is_closed_by_employee" invisible="1"/>
                        <field name="is_my_message"         invisible="1"/>
                        <field name="mail_sent"             invisible="1"/>
                        <field name="closed_date"           invisible="1"/>
                    </sheet>
                    <chatter/>
                </form>
            </field>
        </record>

        <record id="action_hr_anonymous_message_employee" model="ir.actions.act_window">
            <field name="name">My Anonymous Messages</field>
            <field name="res_model">hr.anonymous.message</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('sender_user_id', '=', uid)]</field>
            <field name="context">{'default_state': 'draft'}</field>
            <field name="search_view_id" ref="view_hr_anonymous_message_search_employee"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first anonymous message
                </p>
                <p>
                    Submit anonymous messages to HR about workplace concerns,
                    suggestions, or any sensitive matters safely and confidentially.
                </p>
            </field>
        </record>

        <record id="action_hr_anonymous_message_employee_view_list"
                model="ir.actions.act_window.view">
            <field name="sequence" eval="1"/>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_hr_anonymous_message_list_employee"/>
            <field name="act_window_id" ref="action_hr_anonymous_message_employee"/>
        </record>

        <record id="action_hr_anonymous_message_employee_view_form"
                model="ir.actions.act_window.view">
            <field name="sequence" eval="2"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_hr_anonymous_message_form_employee"/>
            <field name="act_window_id" ref="action_hr_anonymous_message_employee"/>
        </record>

        <menuitem id="menu_anonymous_message_root"
                  name="Anonymous HR"
                  sequence="50"/>

        <menuitem id="menu_anonymous_message_employee"
                  name="My Messages"
                  parent="menu_anonymous_message_root"
                  action="action_hr_anonymous_message_employee"
                  sequence="10"/>

    </data>
</odoo>